import React from "react";
import { useFetchCosts } from "../../hooks/useFetchCosts";

const MemberStats = () => {
  return (
    <>
      <h2>Members of the group</h2>
      {/* display name, and percent */}
    </>
  );
};

export default MemberStats;
